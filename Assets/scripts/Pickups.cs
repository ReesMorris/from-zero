using UnityEngine;

public class Pickups {

	public enum Type { Gun, Health };
	public enum Slot { Primary, Secondary, Melee, Grenade, HealthItem };
	public enum DropDirection {Forward, Left, Right};

}
